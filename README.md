# Weather-Shell
Weather in terminal maked with Shell script.
## Type ```sudo ln -s ./weather.sh /usr/local/bin/weather && sudo ln -s ./weathera.sh /usr/local/bin/weathera``` to use weather and weathera in any directory.
